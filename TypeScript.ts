let message: string = 'Hello World';
console.log(message);

// To compile typescript code we can run the following command on the command line.
// tsc TypeScript.ts
// This command will generate a javascript file with name TypeScript.js

// Run the javascript file using the following command on command line:
// node TypeScript.js
